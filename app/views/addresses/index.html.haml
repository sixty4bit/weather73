%h1 Listing addresses

%table
  %thead
    %tr
      %th Formatted address
      %th Postal code
      %th Place
      %th Latitude
      %th Longitude
      %th
      %th
      %th

  %tbody
    - @addresses.each do |address|
      %tr
        %td= address.formatted_address
        %td= address.postal_code
        %td= address.place_id
        %td= address.latitude
        %td= address.longitude
        %td= link_to 'Show', address
        %td= link_to 'Edit', edit_address_path(address)
        %td= link_to 'Destroy', address, method: :delete, data: { turbo_method: 'delete', confirm: 'Are you sure?' }

%br

= link_to 'New Address', new_address_path
